name: lxweb
version: "0.1.0"
description: "Framework web para Lx — roteador, controllers, views e Live"

# Adapter packages are separate:
# - lxweb_cowboy: adapters/cowboy
#   requires cowboy ~> 2.0

dependencies:
  cowboy:
    version: "~> 2.0"
  ranch:
    version: "~> 1.8"
  cowlib:
    version: "~> 2.11"
  jsx:
    version: "~> 3.1"
dev_dependencies: {}
